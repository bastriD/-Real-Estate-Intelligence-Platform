apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: real-estate-observability
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://gitlab.local/root/lab-gitops.git
    targetRevision: main
    path: workloads/monitoring/real-estate

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

    syncOptions:
      - CreateNamespace=true